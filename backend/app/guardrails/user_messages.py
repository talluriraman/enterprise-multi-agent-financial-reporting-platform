"""Short user-facing strings (avoid long markdown in the primary UI)."""

# Shown when prompt fails financial validation (guidance path) — matches financial agent minimal-context wording.
VALIDATION_FAILED_SHORT = "No expense-specific keywords; returning minimal context."
